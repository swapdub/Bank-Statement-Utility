# Example .env for FastAPI backend
# In normal use, env vars are inherited from web/.env via run.sh or docker-compose env_file.
#
# To restrict CORS from the default open "*" (advanced):
# ALLOWED_ORIGINS=http://192.168.1.100:5173,https://myapp.com
